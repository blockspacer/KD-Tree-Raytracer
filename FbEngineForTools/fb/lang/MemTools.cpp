#include "Precompiled.h"
#include "MemTools.h"

#if FB_MEMTOOLS_ENABLED == FB_FALSE
	/* Make sure this is compiled in. Pretty much impossible to miss, but it's the principle of things */
	#include <memory>
#endif

FB_PACKAGE1(lang)

/* There used to be stuff here. No more. */

FB_END_PACKAGE1()
